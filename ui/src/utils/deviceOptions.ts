export const inputOptions = Array.from({ length: 11 }, (_, i) => ({
  label: `X${i}`,
  value: `X${i}`,
}));

export const outputOptions = Array.from({ length: 11 }, (_, i) => ({
  label: `Y${i}`,
  value: `Y${i}`,
}));